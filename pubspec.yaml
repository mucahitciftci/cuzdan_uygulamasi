name: cuzdan_uygulamasi
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.12.2'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  easy_localization: ^3.0.7
  fl_chart: ^1.2.0
  shared_preferences: ^2.5.5
  
  # --- HIVE VERİ TABANI PAKETLERİ ---
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  
  # --- HIVE KOD ÜRETİCİLERİ (Modelleri otomatik bağlamak için) ---
  build_runner: ^2.4.9
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/lang/
    - assets/images/ic_wallet_blue.png